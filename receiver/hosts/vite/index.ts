/**
 * tracelink-vite public entry.
 *
 * **RECEIVER** — Vite middleware tool.
 * Dimension: receiver · tool axis (how the debug server is built: Vite middleware)
 */

export { debugLogPlugin } from './plugin.js';
export type { DebugLogPluginOptions } from './types.js';
